const INITIAL_STATE = {
    cartItems: [],
    newCartCount: 0
}

export const dataReducer = (state = INITIAL_STATE, action) => {
    switch(action.type){
        default:
            return state;
    }
}